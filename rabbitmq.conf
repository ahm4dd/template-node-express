# Stop using deprecated management metrics collection (no UI charts; use Prometheus instead).
management_agent.disable_metrics_collector = true

# Enforce "as if removed" for the deprecated feature.
deprecated_features.permit.management_metrics_collection = false
